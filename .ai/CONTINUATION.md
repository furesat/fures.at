# AI Continuation State

## Last Completed Phase

Added the Maria Alm Digital Discovery Atlas / `inmariaalm.at` project entry to the Furkan Yonat CV microsite project list in all supported CV languages (TR/EN/DE/ES), then rebuilt and committed the change.

## Current Project Status

The repository remains a Netlify-deployed public static/multilingual site using Vite + React for the main client application and Eleventy for RSS/sitemap/static output. The current change is scoped to the existing `/furkanyonat` CV microsite content data and its built public output generated by `npm run build`.

## Next Phase

Optional next phase: if the owner wants this Maria Alm project represented beyond the CV and existing main-site external card, create a dedicated static case-study page and add it to the sitemap generator. Do not do this without explicit scope because it creates a new indexable public route.

## Remaining Phases

1. Optional: add missing public-site `robots.txt` with `Sitemap: https://fures.at/sitemap.xml` and appropriate allow/disallow rules.
2. Optional: add root `.env.example` with safe placeholders for `VITE_SITE_URL`, Gemini, Maps, and optional image provider keys.
3. Optional: consider adding standalone `typecheck`, `lint`, or validation scripts if the project owner wants stronger CI checks.
4. Optional: create a dedicated Maria Alm case-study page if explicitly requested.

## Important Files

- `AGENTS.md`
- `furkanyonat/AGENTS.md`
- `.ai/CONTINUATION.md`
- `README.md`
- `package.json`
- `netlify.toml`
- `src/sitemap.xml.njk`
- `furkanyonat/data/translations.ts`
- `furkanyonat/components/Projects.tsx`
- `furkanyonat/components/ui/ProjectCard.tsx`
- `public/furkanyonat/index.html`

## Commands Verified

- `find .. -name AGENTS.md -print` — checked agent guides; found root `AGENTS.md` and nested `furkanyonat/AGENTS.md`.
- `git status --short --branch` — checked current branch and working tree before editing.
- `git log -3 --oneline` — inspected latest commits.
- `sed -n ... README.md package.json netlify.toml src/sitemap.xml.njk furkanyonat/...` — inspected documentation, scripts, deployment redirects, sitemap setup, and CV project rendering/data files.
- `find . -maxdepth 3 -iname '*robots*' -o -name 'sitemap.xml'` — checked robots/sitemap files; dynamic sitemap template exists and no root/public robots file was found.
- `npm run build` — passed; generated `public/furkanyonat/index.html` and existing build artifacts.

## Known Risks

- No root/public `robots.txt` file was found; this remains technical debt for a public website.
- The Maria Alm project is linked externally (`https://inmariaalm.at`) from the CV project grid, so no new fures.at route was added in this task.
- The main site `src/components/Projects.tsx` still marks `maria-alm-route-atlas` as under construction, per root business rule. Do not remove that status until the project owner confirms it is genuinely live.
- Root `package.json` has no `lint`, `test`, or standalone `typecheck` script; `npm run build` is the available production validation and includes `tsc`.

## Do Not Do

- Do not invent translations, routes, business rules, or metrics.
- Do not create duplicate SEO, sitemap, RSS, form, or automation systems.
- Do not bypass existing `useSEO`, route mapping, Eleventy data, or Netlify Forms patterns.
- Do not skip SEO/sitemap/robots checks on public-page tasks.
- Do not store real secrets, API keys, tokens, passwords, or sensitive data.
- Do not hand-edit built `aboutcyprus/` assets when the source change belongs in `travel/` unless explicitly necessary.
- Do not ignore nested `AGENTS.md` files, especially under `furkanyonat/`.

## Required First Step For Next Agent

The next agent must first read:

1. `AGENTS.md`
2. `.ai/CONTINUATION.md` or `AI_STATE.md`
3. latest commit history
4. relevant source files for the next phase

Only after reading these should the next agent plan or modify code.
